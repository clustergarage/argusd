syntax = "proto3";

package fim;

service Fimd {
  rpc NewWatch(FimdConfig) returns (FimdHandle) {}
}

message FimdConfig {
  string containerId = 1;
  repeated FimWatcherSubject subjects = 2;
}

message FimWatcherSubject {
  repeated string paths = 1;
  repeated string events = 2;
}

message FimdHandle {
  int32 id = 1;
}
